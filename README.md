# Poker Preflop Trainer

Static web app for memorizing Yokosawa-style preflop hand ranges.

## Run locally

```sh
python3 -m http.server 5177 --bind 127.0.0.1
```

Open `http://127.0.0.1:5177/`.

## Deploy

This is a static site. It can be deployed with GitHub Pages from the repository root.

